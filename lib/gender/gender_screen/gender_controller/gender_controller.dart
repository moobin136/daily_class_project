import 'package:flutter/material.dart';

class GenderController extends StatelessWidget {
  const GenderController({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
